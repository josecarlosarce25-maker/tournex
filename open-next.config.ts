// OpenNext.js adapter config for Cloudflare Workers.
// Tournex runs as a single Worker that serves SSR + API + static assets.
import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig();
